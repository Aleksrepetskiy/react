"use strict";
let a = 4;
let b = 'some';
let c = 'hello';
let j = true;
let arr = ['dsds', 'eweww'];
let e = 3;
function test(a) {
    return '';
}
const test2 = (a) => {
    return a * 2;
};
//Если ничего не возвращает то void
const test3 = (a) => {
    return;
};
arr = arr.map((x) => x.toLowerCase());
function countCord(cord) {
}
function printed(id) {
    if (typeof id === 'number') {
        console.log(id.toString());
    }
    else if (typeof id === 'string') {
        console.log(id.toUpperCase());
    }
}
function getSum(a) {
    if (Array.isArray(a)) {
    }
}
const x = undefined;
const z = null;
